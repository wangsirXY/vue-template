import Vue from "vue";
import Vuex from "vuex";

Vue.use(Vuex);

export default new Vuex.Store({
  state: {
    // 源数据
  },
  getters: {
    // 更新state
  },
  mutations: {
    // 修改state
  },
  actions: {
    // 异步mutations
  },
  modules: {},
});
